// function missingNumber(x: number[]): number {
//     let n = x.length;
//     let expectedSum = n * ( n + 1) / 2;
//     let actualSum = x.reduce((sum, num) => sum + num, 0);
//     return expectedSum - actualSum;
// }

// console.log(missingNumber([3, 0, 1])) // 2
